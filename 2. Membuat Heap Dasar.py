import heapq

# Membuat heap
numbers = [5, 3, 8, 1, 2]
heapq.heapify(numbers)

print("Heap yang dibuat:", numbers)
# Fungsi: Mengubah list menjadi heap.
# Kondisi: Ketika Anda ingin menggunakan struktur data heap untuk mendapatkan elemen dengan prioritas tertinggi.
